const sep = '-';
const right = 'right';
const up = 'up';
const left = 'left';
const down = 'down';
const left_up = left + sep + up;
const right_up = right +sep+ up;
const right_down = right + sep + down;
const left_down = left + sep + down;
const default_img_url = 'images/1.jpg';
const maxWidth = 600;
const maxHeight = 400;

export let context = {
    sep,
    right,
    up,
    left,
    down,
    left_up,
    right_up,
    right_down,
    left_down,
    default_img_url,
    maxWidth,
    maxHeight
};